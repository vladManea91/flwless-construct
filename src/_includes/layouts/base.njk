<!doctype html>
<html lang="ro">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">

{% set titluPagina = seo_titlu or titlu or title %}
{% set descrierePagina = seo_descriere or descriere or site.descriere %}
{% set imaginePagina = seo_imagine or site.og_imagine %}

<title>{% if titluPagina %}{{ titluPagina }} · {{ site.nume }}{% else %}{{ site.nume }} · {{ site.tagline }}{% endif %}</title>
<meta name="description" content="{{ descrierePagina }}">
<link rel="canonical" href="{{ site.url }}{{ page.url }}">

<meta property="og:type" content="website">
<meta property="og:locale" content="ro_RO">
<meta property="og:site_name" content="{{ site.nume }}">
<meta property="og:title" content="{% if titluPagina %}{{ titluPagina }} · {{ site.nume }}{% else %}{{ site.nume }}{% endif %}">
<meta property="og:description" content="{{ descrierePagina }}">
<meta property="og:url" content="{{ site.url }}{{ page.url }}">
{% if imaginePagina %}<meta property="og:image" content="{{ site.url }}{{ imaginePagina }}">{% endif %}
<meta name="twitter:card" content="summary_large_image">

{% if noindex %}<meta name="robots" content="noindex, nofollow">{% endif %}

<link rel="icon" href="/images/brand/favicon-32.png" sizes="32x32" type="image/png">
<link rel="icon" href="/images/brand/favicon-512.png" sizes="512x512" type="image/png">
<link rel="apple-touch-icon" href="/images/brand/apple-touch-icon.png">
<meta name="theme-color" content="{{ site.culoare_text or '#1b1b1d' }}">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Archivo:wght@400;500;600;700&family=Literata:opsz,wght@7..72,400;500;600&display=swap" rel="stylesheet">
<link rel="stylesheet" href="/assets/css/style.css?v={{ assets.css }}">

<style>
:root {
  --ink: {{ site.culoare_text or "#16202a" }};
  --accent: {{ site.culoare_accent or "#a97b2f" }};
  --paper: {{ site.culoare_fundal or "#f3f3f0" }};
}
</style>

<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "HomeAndConstructionBusiness",
  "name": {{ site.nume | json | safe }},
  "description": {{ site.descriere | json | safe }},
  "url": {{ site.url | json | safe }},
  "telephone": {{ site.telefon | json | safe }},
  "email": {{ site.email | json | safe }},
  "areaServed": {{ site.oras | json | safe }},
  "image": {{ (site.url + (site.og_imagine or "")) | json | safe }}
}
</script>
</head>
<body>

<a class="skip" href="#continut">Sari la conținut</a>

{% include "partials/antet.njk" %}

<main id="continut">
  {{ content | safe }}
</main>

{% include "partials/subsol.njk" %}
{% include "partials/bara-mobil.njk" %}

<script src="/assets/js/site.js?v={{ assets.siteJs }}" defer></script>
{% if site.analitice_interne %}<script src="/assets/js/analitice.js?v={{ assets.analiticeJs }}" defer></script>{% endif %}
</body>
</html>
